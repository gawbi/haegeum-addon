# haegeum-addon — CPU 재현 환경
# 저장소의 .py / .ipynb 가 실제로 import 하는 서드파티 패키지 전수 (버전 고정).
# 벤치마크·SHAP 분석 실측에 사용한 호스트 버전과 동일하게 맞춰져 있습니다.
#
# ROS2 노드(anomaly_detector / red_agent / target_fusion / sensor_publisher /
# ugv_agent)가 쓰는 rclpy · std_msgs · geometry_msgs · haegeum_interfaces 는
# pip 패키지가 아니라 ROS2 배포판에서 제공됩니다(apt / colcon). 따라서 여기에
# 넣지 않으며, benchmarks/ 와 explainability/ 는 utils/load_model.py 의 스텁으로
# ROS2 없이도 같은 VAE·가중치를 로드합니다.

# 핵심
torch==2.11.0            # VAE 정의·추론·동적 양자화 (CPU)
numpy==2.4.3             # 센서 시계열 생성·윈도잉·통계
matplotlib==3.10.8       # 결과 플롯 (Agg 백엔드)

# 설명가능성 / 평가
shap==0.52.0             # KernelExplainer 기반 재구성 오차 기여도 분해
scipy==1.17.1            # spearmanr (SHAP ↔ 재구성 오차 기여도 순위 상관)
scikit-learn==1.8.0      # shap.kmeans 배경 분포 요약, 노트북 평가 지표
